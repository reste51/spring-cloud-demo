spring:
  application:
    # 注册中心的名称
    name: eureka-server

eureka:
  client:
    service-url:
      # 注册中心的 页面地址
      defaultZone: http://localhost:8888/eureka/
      # 不注册中心中
    register-with-eureka: false
#    fetch-registry: false

# 访问注册中心的 页面端口号
server:
  port: 8888






